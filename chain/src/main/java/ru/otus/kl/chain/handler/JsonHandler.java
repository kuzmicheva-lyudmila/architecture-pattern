package ru.otus.kl.chain.handler;

public class JsonHandler extends Handler {

    private static final Extension EXTENSION = Extension.JSON;

    public JsonHandler() {
        super(EXTENSION);
    }
}
